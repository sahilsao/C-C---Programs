/*Reading a series of words using'scanf'function*/
#include<stdio.h>
#include<conio.h>
void main()
{
char word[40];
puts("enter the string:");
gets(word);
puts("entered string is:");
puts(word);
getch();
}